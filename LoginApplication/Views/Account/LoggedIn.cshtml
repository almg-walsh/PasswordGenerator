@model LoginApplication.Models.UserAccount

@{
    ViewBag.Title = "LoggedIn";
}

@if (Session["UserId"] != null)
{
    <h4>Hello @Session["UserId"].ToString()</h4>

    <p>Here is your password <b>@Model.Password</b></p>
}

